using UnityEngine;

public class FuelItem : MonoBehaviour
{

    // num of uses
    public float fuelAmount = 3;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
